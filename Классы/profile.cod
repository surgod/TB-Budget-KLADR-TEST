class "Profile";
-- в этом проекте выполняется загрузка КЛАДР
-- используется
-- Базовый.Бланки.Сервис.блЗагрузкаКлассификаторов

inclass

  proc preinit;
  
  end;

  proc init;
   openblank("Управление.блТестКладр");
  end;

  proc GetSessionInfo;
    AddSessionInfo(СИС2.Константы.ЗаголовокИнформацииОРелизахТББ, 'Версия проекта ' + ClassProject + ' : ' + Str(Константы.ПолучитьВерсиюПроекта, '0.000'));
  end;


end